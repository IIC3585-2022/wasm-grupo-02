{
  "name": "wasm",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "run-p build:wasm && run-p build:application",
    "build:wasm": "make clean && make build",
    "build:application": "vite build",
    "watch:build:wasm": "watch 'make clean && make build' lib",
    "preview": "vite preview"
  },
  "dependencies": {
    "vite": "^2.9.5"
  },
  "devDependencies": {
    "eslint": "^8.14.0",
    "eslint-config-airbnb": "^19.0.4",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-import-resolver-alias": "^1.1.2",
    "eslint-plugin-import": "^2.26.0",
    "npm-run-all": "^4.1.5",
    "watch": "^0.13.0"
  }
}
